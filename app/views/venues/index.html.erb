<h1>Venues#index</h1>
<p>Find me in app/views/venues/index.html.erb</p>
<% @venues.each do |v| %>
<p><%=link_to v.name, venue_path(v) %><%= link_to 'Add a performance', new_venue_performance_path(v) %> </p>
<%end%>
<%= link_to 'Add your venue', new_venue_path %> </p>


<div x-ng-app="songkick-app">
	<div x-ng-controller="venueCtrl">
		{{metroAreaId}}
		{{displayNameOne}}
		{{venueNameOne}}
		{{artistNames}}
		{{venueNames}}
	</div>
	<div x-ng-controller="calendarCtrl">
	</div>
</div>

